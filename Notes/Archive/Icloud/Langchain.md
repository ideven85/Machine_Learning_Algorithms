# Langchain    
## Transcript  
**00:00:00 - 00:01:06**  
  
**Introduction to LAN Chain Framework**  
  
OK, no problem, let's get started. So basically in the previous session that is session one, we have understood what is LAN chain. LAN chain is a open source framework used to build the models like you can give an input prompt, you can get output in a very past format. Let's say you want in Jason, you wanted CSV, anything you can get it. It is also supporting multiple LLMS like Open, Open AI, Hugging Face, Google, multiple LLMS, whatever are ready. Multiples are available using line chain. So it supports that. We have seen the first part where how we import a query, how we can use dot generate and give multiple queries to it and it can generate the response for you. Then we have seen that there are human message system message and AI message for chat bot where you can define a human message, system message and then you can mimic a chat bot. We have seen that OK, then there are templates.  
  
**00:01:06 - 00:01:48**  
  
**Overview of Previous Session Topics**  
  
We have seen how to create a prompt template. You can give here input variables and you can generate a prompt template there. OK. Now after that chat prompt template where again input and output that user can change according to the application and you can generate it. Following this we have also seen how to save your prompts and then how to create your few short templates. So this everything we have seen in a lot of examples and many of you have tried that in the session itself also. Now what we are going to cover today, today we are going to cover a lot, lot of interesting things.  
  
**00:01:50 - 00:02:39**  
  
**Today's Session Overview**  
  
Let me take you to that, OK, Now this session we will try, we will start the session with chunks, OK, You have seen some part of it in the module, so we will start with chunks. Just a second. So what we are going to cover today, we are going to see what is chunk today along with that we are going to see what are embeddings. So some part you have already seen in your module, right? Everyone has done that in your module embeddings, how to generate it and how to use vector database, correct?  
  
**00:02:40 - 00:03:06**  
  
**Installation of Required Libraries**  
  
Yes. But have you used that line chain perfectly low? OK, so today you are going to see how to use a line chain to do all this stuff, embeddings, chunks, what is vector store, how to use a vector DB? OK, then we are going to see chain different type of chains and we are going to see agents today. So this is what we are going to cover. It would be really very, very, very interesting.  
  
**00:03:07 - 00:03:40**  
  
**Setting Up OpenAI Key**  
  
Let's start with the first part for today. Wait just a second. OK, now first on the foremost thing is let's start with the pouring part of it because maximum thing here would be pouring only. So we will install some basic things first of all like we will need line chain and open AI and chroma DB.  
  
**00:03:40 - 00:06:14**  
  
**CSV Loader in LAN Chain**  
  
Also we are working OK so we have a lot of things. Let's quickly run this install. So we will be needing sentence transformer, doc array, chroma DB PDF, 5 PDF. We are going to see everything this LAN chain, chroma DB this all things we are going to see. So let it install all the stuff. First, we will start with importing of libraries like import OS and we will need import open AI. These two libraries we will need. With this, we will open our open AI key with open open AI key and let me upload the open AI key here. OK, now I can simply set that. You know this basics setup that requires we open a text file, we read that and finally we set that as an environment variable, our open AI key. Once this basic thing is done, let's get started with the LAN chain. Now the first and the foremost part is LAN chain and what you are going to see today. First, we are going to see how we can start with CSV loader here, What is CSV Loader and how to get started with that. So let's understand what is CSV loader in line chain now. So first of all we need to import it from linechain document loaders. Import CSV loader. OK now what does this CSV loader does? Up till now we were all playing with clang chain also using text only like we used to write. Tell me about this restaurant, tell me about who is the president of India and all that. We are playing with text but now we will need a CSV file. So for that we are going to use a CSV loader. We will import a CSV file here I will upload all of the file. Let's upload our laptop data that you have used a lot in your projects. Laptops data_new dot CSV.  
  
**00:06:14 - 00:09:04**  
  
**PDF Loader in LAN Chain**  
  
So in this CSV loader you just need to pass this file name that is laptop_data_new dot CSV and we will save that in an object called as loader and now. You can use this loader object to load the data OK let's. STORE, that in data ready that's it. Now let's. Understand what is the use of this loading the data and how it is used so we. Can just mention here data 0 let's look. At the data first index and you will see that we will have a complete row inside it. OK, so page content is the file, the name of the laptop that is Intel I511 Core 11 Gen. then the core. So here the values are separated by slash insurance. As you can see core called core, then the clock speed, then the RAM, then the storage graphics card and all that stuff. Same way the complete CSV file that we have, it's completely loaded here. If you don't want to see it like this format you can use dot page content. Now just keep this in mind, this is very important dot page content. This will give you a data in a very proper format. Just we can use a print on top of it. OK, instead of giving this document object, it will give you a complete in a string format where you can pass this to a print function and you get in a very systematic format like name, core clock speed, RAM, storage, graphics and all that. So keep in mind page content is used to extract the data from document type. Simple. Now similarly along with this you have different type of loaders also. For example, let me show you one more loader here and that is PDF loader. So we will see what is PDF loader now in which we you can load a PDF file. So let's first load a PDF file here so that we can use it launching. We have a PDF file policy documents. Yeah, this is one of the PDF file. Yeah, I can see the PDF one. Yeah, this is a PDF file.  
  
**00:09:04 - 00:13:07**  
  
**Text Splitters and Chunking**  
  
Let's use this Python intro itself. Let's see what is there in this PDF file, OK. And how you can load it in the same way like you loaded your different. So like you loaded your CSE file in the same way you can load the PDF also. So we will just mention here, just give me a second. I'm connecting my pen, yeah. So this is getting loaded. We can just simply use here from line chain dot document loaders in the same way import π PDF loader OK similar to that like you have created the object here using the CSV name same way we need to do here also PDF loader and this is taking too long. Let's upload another PDF. Yeah, this is a small one. Let's upload this. This is taking long rides. Just a second. Yeah, it is uploaded. So in the PDF loader you can mention the PDF name. Let's say I have mentioned it here. hello.pdf OK. And you can save that in a object let's say PDF loader. And to use a PDF loader like you have used here 0. And you got the first element of your CSV same way here also you can do it PDF loader. And you need to mention here dot load and split. OK dot load and split here what we have done in your CSV one directly load here you need to load and split method OK, You can mention that in pages. You can save that in pages so that we can use it step by step, OK. Now, once you have done this in the same way like you mentioned here, data 0, same way we can mention here pages 0 and you will get exactly the similar type of behavior here, OK, like document what is written inside the document, What is that policy about the complete first page? You will get it. If you want to look it again in a very bitter beautiful way, you can use the print command and you can get that OK. So it will work perfectly fine. OK, Now the complete page is rendered here. Isn't that amazing everyone? Is that super clear how to use PDF loader and CSP loader? Make sense? Super super super.  
  
**00:13:08 - 00:16:00**  
  
**Character Text Splitter**  
  
Now moving to the next point. Now we have understood how these two things work. The next portion, very interesting. Let's get started with the most amazing part that you are going to use in your lamp chain is X splitters. Now first of all, why do we need X splitters? Anyone you have completed in your module tell me why do we need text leaders? Very good, perfect. It is used for chunking. But why do we need chunking? So basically we all know that we have already used a lot of things in ChatGPT up till now everyone has used yes that's right. Because LLMS right now have the limits. Limits in sense you have a fixed token size. For example, in ChatGPT 3.5, you can have, let's say 16 K tokens, OK, in your GPT 4, you can have 32K tokens, which means that there is a limit the data that you can process in one go. So that's the reason whatever text we have, OK, the big text, we create chunks out of it. Let's say this is 1 chunk, this is another chunk and let's say this is another chunk. So this is called as split one, split two and split three. Now you process split one, split 2, split three separately so that you are inside the limits of your GPT or the model that is there. Now text splitter is exactly used for that. Now we will open one text file. Also, let's quickly upload one of the text file as well here so that we can use it. So let's say this fixed TXT file. OK, it started uploaded very quickly. Let's move and let's open that file. So you can simply open the file using this command with open fixed TXTSF and let's read the content in type inside it. These are the top first 100 characters of text token. Kevin Helger suggested an interesting way to compare programming language. We can change the complete text. This is a small one only it has suggested something it has mentioned about some language. So it looks very good.  
  
**00:16:01 - 00:26:03**  
  
**Recursive Text Splitter**  
  
Now this is our complete text content. How to split this? Now let's see that to split this you need to use the first method in from langchain dot text_splitter import character text splitter. OK now in this character text splitter you can just easily pass a splitter. So for example I want to split this text based on NI want every sentence in a In a new chunk. So I will mention here my Sep rater separator equals to N OK if you want you can change this and I will show you how to change this. Also chunk size equals to 100. Now this is very important the chunk size and then the chunk_overlap and I have given here overlap of 10 chunks. Now after defining this we can store in an object character text splitter. OK, now we are going to use this character text splitter equals to dot create_documents and inside that we will pass our text content, OK. And that if you want to pass here, if you try to pass it directly it will just give it will take every single character here. So this is not a right format. You need to pass that in a list format OK like this. Keep in mind because if you do in a normal format without list it will considered every character as your chunk. So after doing this we will save that in the character text splits split text. So we have that character split text. Now let's see what we have inside this. Now let's understand how this character splitter works. So basically The thing is very simple here. Imagine that you have here sentence one. Imagine that you have sentence one that is of 70 characters. Then you have sentence 2 of 35 characters. OK now and between these two sentence there is slash N Next thing when you define that OK in my character text splitter I want it to split on slash N and you mentioned the chunk size as 100. So first of all the first sentence it would check is that less than 100? Yes. So sentence one would come. Now the moment you try to add another sentence here, when you go to next sentence, it will take the complete sentence and it will check 70 tokens are done here and these are plus 35 tokens. Tell me it is less than 100. No, because of that because it is crossing the limit. OK, the sentence to will not be allowed to be the part of the first split. So split number one would only have sentence one if it doesn't match the criteria with sentence 2. So same thing has happened here. Let's see that Kevin Telger suggested an interesting way to compare programming. OK, so this became a chunk size of whatever it is. Now if they try to add the next part that is languages to describe. Now see after compare programming there is slash in. So this became your sum size of chunk and after this to the next part that is still it. You can see that from languages to it, it became another chunk because combining this Kevin Kalger and this chunk would be going more than 100. So this part came down. OK, everyone clear with this simple chat splitter, yes or no, everyone variable. Now same thing. You can change the splitter here and I can mention space there. OK, and based on space, now it will chunk. Now see the difference in both the chunking methods. Here it stopped at programming. OK, here you see it has stopped at programming. But in this case now it will not stop at programming. Just look at it. It has Now Kevin Kelger suggested the interesting way to compare programming language to describe each in after in it has covered 100 tokens and it has stopped there. So in this way you can have different type of splitters here. OK, warning says simple that created a chunk size of 139 which is longer than the specified. So these are some warnings. OK, you can ignore it. This in some cases it has given like one or two documents, it has make more than 100 also. OK, so that warning it has given you can increase or decrease the chunk size that it can remove this warning also. OK any doubt Narayana. So the chunk size is given is given priority. See, I would show you that for example, if we take this first chunk, let's take start from Kevin and this part OK, so the chunk size here is defined as 100. Now if I try to check the size here, now this would not give you the perfect one. But yes, you can see that till compare programming it is 66. And if we take the next part and we check the length of that OK, 66 + 54, it is going much more than 100. So they are not kept in same line. And now see the next one this Kevin to this part, it is going till 97. OK, I've already checked this. The length of this is now 97. OK. Now if it tries to add one more word here after space, the word is each. So 97 + 4, that is 1/01. That's the reason each came in the next line. Now here we got the warning. Why? Because some chunk OK, which is greater than 100. So let's see one of the chunk which is greater than 100. Now it would not have a separator in between that slash N, So that's the reason it needed to do it. OK, 189 so one 89189 something is the chunk size here. Just a second. OK, so this content like I said you it's going above 100 because there is no slash and in between it cannot break it. So you are getting that warning. Got it, very good. OK, so coming to the next point, this splitter is important to split the documents. Now we will see the embeddings here and one more splitter. So this is one of the basic splitter that you have seen. I can just print all the text here so that you understand it better. Character text splitter here. So for first page content then the length of it page content you can check that OK so the first is 97 the next one is exact 100 then this one is 96 OK then after that 100 then again 99 and 97. Now in this none of it got above 100 so there is no warning. This is your simple characters splitter. Based on what character you have given it will split that.  
  
**00:26:04 - 00:29:30**  
  
**Text Embeddings with OpenAI**  
  
Now the most important one that you are going to use in your industry is recursive text splitter. Very very, very very important and you will understand why this is important. OK so now we will load that in the same way like we have done from Langchain text splitter and both recursive character text splitter. We will create an object of it. We will pass here the chunk size and overlap exactly same. Let's not change it. We got the object created here that is text splitter. Now you can easily use this text splitter dot text splitter dot create documents. And inside that in the same way list and then text_content. Now let's understand what's the difference on this and how we are going to use this and we'll save that in text splitter. Now text splitter you will see that the difference here is you have the just a second we have taken here programming language. Just a minute. So let's print all the documents that we have here. So we have right now total 17 splits. Let's try to make some splits here and see the difference.  
  
**00:30:02 - 00:36:05**  
  
**Vector Store and ChromaDB**  
  
Now first of all, what recursive text splitter does is preferred with generate text and recursively tries to split the text until the chunks are small enough. So first of all it will try. So for example, this is your text document. Let's go back to our text document. Yeah, so in this complete text document, first it will split on double slash N wherever it will find the double slash NS. OK, now if you see here, there is here it is double slash N but this complete part is going beyond 100. So the next one, it tries to check if slash N is working here. OK, so it will split the text on slash north. Now slashing it's working perfectly. You have less than 100 characters. So you got here less than 100 characters. OK, you can see this. Let's try to little bit change this so that you understand it better. Let's take the text content and let's try to remap it. None. Just a second, I am coming on now. See this is what now you will see the different sector. So basically like I said you it is recursive text splitter. Now what do you mean by recursive text splitter? Recursive means that it has a sum of the text splits like slash in, slash in. Then you have a single slash in, then you have with space and then you have with without space. These are the four splitters that you have split one split two and then three and four. Now for example, you have a text where you have word one word 2 like this and then you have double slash ends this one. OK, so now first of all this recursive recursive text splitter, it will check for this one that this split is within the length of your even chunk size. My chunk size is 100 if this is less than 100 let's say 828385 whatever it is so it will take this first part. But let's imagine that this is not the case. Now I have a text OK and that text have a slash in at very low point so which means you have word one word 2 till this word 100 then word 23 like 101 like that. Now tell me in your normal splitters everyone agree that chunk size would be greater than what we said and you will get warnings, correct? We have seen that before. Yes or no, everyone quick. Yes, perfectly right, We have seen it. So that would happen, OK. And you can get more than chunk size that we said in your recursive text splitter. What it will do instead of going till slash in, slash in it will go to now next split because the first splitter is not working. It will see from this word to this word Is there any slash in? Next slash in is present but slash in is present at 100 and this is 110. OK at 1:10 there is slashing again. This doesn't match our criteria of chunk size 100. Tell me yes or no again it does not match. So now it will not use this slash and also as a separator. So we are using multiple separators in one go instead of that. Now it will move to the space. Now it will see this is chunk 1, chunk till chunk 100 it will go and then it will create this split for you. So you don't need to mention any character one by one it will check and 1 by 1 you will get the values here. OK now see in the same scenario I have explained you in the first go we had slash and slash and at very bottom the original text see slash and slash and is here. Everyone agree that this big chunk is greater than 100 Yes or no? Yes, because it is greater than 100, we cannot use this slash and so it goes back and it sees that OK slash in this is less than 100. So that's the reason when you use character splitter, recursive character splitter on this text, it first takes for double slash in then it stops at single slash in. So you see that till programming you have the first one OK in a normal one. But now what I did I changed the double slash in and kept it within the limits.  
  
**00:36:06 - 00:45:00**  
  
**Sequential Chains in LAN Chain**  
  
So you see that the first split is tildescribe function OK then second one it starts from here and there is no double slashing. So will it give you warning? No, it will check from double slash in to next slash in. This is less than 100 so you got this as 31. Is that super clear everyone? The recursive text, How it is used? Why it is used and how to use that super clear quick yes or no? OK, very good guys. This speed is good. Should I increase the speed, decrease the speed? Is it going perfect or you want me to increase or decrease it quick? Anything we can make it good? Fine. OK, everyone. I just want the collective here. We'll go with majority. Good, perfect. Good. OK, very good. Coming to the next part. OK, thank you so much everyone. Let's see the next part and then I will give you time to practice this, the text embedding models. Now for this we will require a tick token and we will use Lang chain now here for open AI embeddings. So from Lang chain dot embeddings import open AI embeddings and we will create an object of this open AI embeddings and that will be your embedding_model embeddings_model. Now we are going to use this embedding model. You know that how to use it embedding model dot embedded_embed_documents and you can provide your document here. OK, let's say I provide my first recursive text split. This part I provided here OK inside this and we cannot mention the IT would be document object. So we need to mention the page content there. So I have mentioned recursive text splits. Let's create the embedding for our first document and this also should be present in the list format. OK. And now we will get the embeddings. Let's store that in embeddings and now we can see the embeddings also here. OK, so anyone remember what is the length of open AI embeddings? Anyone remember it? It's 15136 in dimension. OK, incorrect API key provided. Let me see. Just give me a second. Let me make it correct. OK, it is done. I will once again load the open AI embeddings. OK, so our embeddings are loaded. Let's run the model once again. OK, now see perfectly it is working. You can see that all the embeddings for our first document are generated and you can see it the length of your embeddings is 15136. This is the most important thing it can be asked in an interview and very, very, very important. The length of embeddings zero OK, so you will see that at 15136 OK Now after doing that we need to store that in a vector database that also you have seen already this is just I am showing you how to store that using bank chain. The vector store to do that. This is also very simple. The vector store you need to from Lang chain dot vector store you need to import chroma. Then you have a complete text splitter that you have generated above the recursive text splitter or your normal text splitter that has document objects. OK we will use this recursive and we will keep this program also less so we will use this recursive text splitter now. So in recursive text letter you would have here documents document, then page content, document type then page content, then document then page content. OK just just a second. Now we can pass this to Chromodb. So we can mention here whatever we have imported, that is CHORMA chroma dot from_documents. And inside this document you will pass your recursive text splits, that is a list of different texts. And then you need to pass which embeddings I'm going to use. So I'm going to use open AI embeddings here. That's it. OK so once you have mentioned this you can mention this as your DB object. And now the last part on this is you will write your query. So you know that why we do all this we can use chroma DB embeddings function as well right? Yes. So you can write a query like say I want to ask in this document what is the author view? Author view about assembly language OK I can mention that and it will try to search the document which will answer this question. OK, you already know that the use of chroma DB and all that some. So we will use DB dot similarities for and we will mention that I want to search for this query. That's it. And you will get the different docs here. The top five docs which are matching to our question. Toma DB has no similarities for SIMINARITY_search. It is similarity_search. OK, now in our docs we will have the top four or three top 4 which are most relevant to our question author's view about assembly language. You can take the first one that would be the most relevant docs 0 dot page content. Assembly language is too low level. Fortune is scary. So in whichever chunk it is coming you will get that chunk. OK, is that super clear? Everyone a big yes or no before I give you this to practice. Everyone very good, very good, very good. So it's now time for a break as well as I'm giving you time to practice this. So before going to the break, you have 5 minutes to practice all the functions. Let me summarize what we have done 1st and then you I will give you this to practice.  
  
**00:45:02 - 00:52:06**  
  
**Router Chains in LAN Chain**  
  
So we started this session with LAN Chen Open Source framework to do all this stuff we cannot use in ChatGPT right now. Like you have how you are using in your ChatGPT or chat completion API. You have done your laptop analysis there laptop recommendation system. In that will you be able to load CSV? No, you were not able to load it. So now we have seen that in Lang chain it's just three line of code. You mentioned Lang chain. You import CSV loader, you create an object for it. Mention your file dot CSV OK. Once you have this object ready you can just simple use here. Let's say this is D so D dot load OK. The load function is important and it would load your CSV file 1 by 1 every every row. Same thing you can do for PDF loader. In the same way you can mention the PDF file namehere.pdf. After this you need to use your load and split. You cannot use a normal load. You need to use load and split here and you will get the complete PDF page by page. After this we started with. After this we started with the chunking part. OK, the different type of splitters, the chunks, we have understood why do we need it and how to use of it. The chunks, there are two types of chunk. The first one is your simple characteristic splitter where whatever character you will mention, let's say space. So it will split on text if you mention slash and it will split on time slash and it will mention it will try to use the chunk size whatever is possible, let's say 102 hundred 300, whatever it is, then it would have overlap as well. What is overlap? So in your first chunk, let's say this is hello, I, how are you? OK, this is your first chunk. So what you say that instead of starting with the next part in the next chunk, you start with some overlapping, let's say R How you doing? I am great. So your model can get a context that this is the first line, this is the second line. So there is some overlap needed. So we mentioned that in the character text splitter. The problem of this is what is the disadvantages? You can have more sentence or more value in your chunk size. So if your transfer is 100 you can get let's say 200 also something like this. OK, now coming to the next plot was a recursive text splitter. We have understood this recursive text splitter is is much advanced than this recursive character text splitter. So what you do in this is first you will check if slash N slash N this is the highest priority. If you have that and the selected text is less than chunk then you will get it. If not it will go to next that is slash N then it will go to space and then it will go to normal without space South. This is your recursive text splitter. After completing this, we have seen that there are text embeddings model. So you can use your text embedding model. We have used your open AI text embedding model and you can just simply create a text embeddings using the function called as open AI embeddings. And in open AI embeddings you just need to pass your complete splitted text that is splitted documents not the exact text but the document. OK And then after that you can use this embeddings in your Chromadb also. So after this we have seen Chromadb. In Chromadb, you need to mention your text splitters, OK? In Chroma, you will mention your recursive split. And after that, you mentioned this open AI embeddings here. OK? So this is what we have seen in your complete part up till now. Now we will start with the next part. But before that, all of you can try this. I'm giving you this right now. OK try this. You need to use your own opinion AI keys and a good work. Is there any hybrid chunking model like combination of character chunking with sentence completion like consecutive new lines? Is there any hybrid chunking model like combination of character chunking with the sentence completion? So which means Narayana, your question is do we have first like it takes the sentence then it can take the characters like that? No, like that there is nothing what I have tested up till now, what I have used up till now. Is this true only so I understood maybe what you mean to say. Let me let me first take the question. So you mean to say yes, I can share that in just a second. So you mean to say here Narayana for example, this is the sentence, it is done on slash in after that can we use the space also something like this like that You mean to say yes. So both of them together would not be possible. Only one at a time. That is what I have seen up till now. Maybe if there is any other advancement or something new came up, not sure. But this is right now which is used. I will check that and will update you if there is something like that. Yeah, let me give you this file now upload the text file and the PA file guys still then you can practice this.  
  
**00:52:06 - 00:59:54**  
  
**Agents in LAN Chain**  
  
I'm quickly uploading this, the text files and all that. OK, so this one has CSVN text file. Yes, everyone can. I see that quickly. Very good. Only three. Where are the rest guys? We are 18 number Dipanshu Ekta, Narayana, Saumya, Saurabh, Saurabh. OK, no problem, let's move to the next point that is chains. Now what is the use of chain? So before I explain you how to do a chain in LLM, let me first show you what do I mean by a chain and why I need that chain. OK now see at this I have just taken my simple ChatGPT screen which I have already used it a lot and we are going to ask some couple of questions to it. OK so now I'm asking this ChatGPT a simple question that suggest me of suggest me the name of an restaurant for restaurant for South South Indian Thai cuisine or men. So I want to open a South Indian cuisine or menu type restaurant. I want a name for that. So when I give it to it, OK, certainly it has given me a name Southern Spice Delight a very good name here. OK, you can write whatever prompt you want. Now what I want is based on this name. Now based on the name decide and create a menu for me which has 10 items and rates are more than normal rate in expensive range. OK, so I have mentioned that you need to create a menu for me, OK, which has 10 items and the rate should be more than normal one, OK, based on this restaurant name, so you can see that certainly it has mentioned something like Mashala Dosa Supreme $16.00 chicken Curry, biryani, awesome Coconut fish Curry, something like that. It has mentioned this. OK, Now if you paid attention guys, all this belongs to New York. Sorry New York, this all belongs to South Indian dish, correct? Yes, you can see that this all belongs to South Indian dish. Now this is acting in terms of a chain. You gave the first input, then you gave the second input and this input is based on your previous output. You can think of any scenarios where you would need this multiple chain. The output of one prompt would go to the next prompt and you keep doing that. OK, so now we are going to code this using line chain, not using an UI that is different. We are going to code this by ourself. So now first of all you need to make a prompt template. OK. So from line chain dot chains import LLM chain and from line chain not LLMS, import open air. OK, there's two things we have imported now. We will create our LLM like we have done before open AI and you can mention let's keep our temperature very high here so that we will get a creative answers 0.9 and then we will try to use whether our LLM is working. So LLM dot predict and inside this I will say I want to open restaurant for veg cool. Suggest an eye-catching name for it in India. OK so I want this answer so let's see what it predicts. It says NLM lamp chain, chain, chain chains, Lang chains, LLM chain. OK so it has given wedge esteem as our name. OK it seems good wedge esteem. Now I will do this using prom template. OK now I have already explained you guys what is from template and how to use that in previous session quickly try that once OK? I will give you 2-3 minutes to try it and then I will explain you how to do it. OK try to write in your own. You can take the previous example also notebook also you already have it and I want this to be converted into a template format. So you have 3 minutes. Everyone try that. None. LangChain is an open-source framework that enables developers to build applications powered by large language models (LLMs). LangChain provides a framework that makes it easier to build LLM-based applications such as:  
* Chatbots and personal assistants  
* Text summarization and analysis  
* Q&A over documents or structured data  
* Code generation and understanding  
* Interaction with APIs  
  
* LangChain works by providing a layer of abstraction between the developer and the LLM. This abstraction layer makes it easier to use the LLM in a variety of ways providing several features that make it easier to build robust and reliable LLM-based applications that can also be LLM agnostic.  
  
* One of the key features of LangChain is its support for chaining prompts. This means that developers can combine multiple prompts together to create more complex and nuanced requests. Another key feature of LangChain is its support for modular components. This means that developers can reuse components from different chains to create new chains. This can save developers a lot of time and effort, and it also makes it easier to share and collaborate on chains.  
  
  
  
## LangChain Framework:   
  
## Overview  
LangChain is a framework that simplifies the development of LLM applications. LangChain offers a suite of tools, components and interfaces that simplify the construction of LLM-centric applications. LangChain provides an LLM class designed for interfacing with various language model providers, such as OpenAI, Cohere and Hugging Face, that makes it easier to build LLM-agnostic applications by simply switching the language models, allowing developers to focus on the application logic without delving into the complexities of dealing with vendor-specific language models. The versatility and flexibility of LangChain enable seamless integration with various data sources, making it a comprehensive solution for creating advanced language model-powered applications.  
  
The open-source framework of LangChain is available to build applications in Python or JavaScript/TypeScript. Its core design principle is composition and modularity. By combining modules and components, one can quickly build complex LLM-based applications. LangChain is an open-source framework that makes it easier to build powerful applications with LLMs relevant to the interests and needs of the user. It connects to external systems to access information required to solve complex problems. It provides abstractions for most of the functionalities needed for building an LLM application and also has integrations that can readily read and write data, reducing the development time of the application. LangChains’s framework allows for building applications that are agnostic to the underlying language model. With its ever-expanding support for various LLMs, LangChain offers a unique value proposition to build applications and iterate continuously.  
  
## Composition  
  
The LangChain framework comprises the following:  
* **Components**: LangChain provides modular abstractions for the components necessary to work with language models. LangChain also has collections of implementations for all these abstractions. The components are designed to be easy to use, regardless of whether you are using the rest of the LangChain framework or not. The illustration below depicts the various components of Langchain.   
* [LangChain](Attachments/390DB0ED-1EC7-494F-BFA7-999D08EC274A.png)  
* Use-Case Specific Chains: Chains can be thought of as assembling these components in particular ways in order to best accomplish a particular use case. These are intended to be a higher-level interface through which people can easily get started with a specific use case. These chains are also designed to be customizable.  
  
The LangChain framework revolves around the following building blocks:  
* Model I/O: Interface with language models (LLMs and Chat Models, Prompts and Output Parsers)  
* Retrieval: Interface with application-specific data (Document loaders, Document transformers, Text embedding models, Vector stores and Retrievers)  
* Chains: Construct sequences/chains of LLM calls  
* Memory: Persist application state among runs of a chain  
* Agents: Let chains choose which tools to use, given high-level directives  
* Callbacks: Log and stream intermediate steps of any chain  
  
The image below from the ++[official documentation](https://python.langchain.com/docs/get_started/introduction)++ provides more information about the Langchain ecosystem. It should be noted that the focus of the session will be only on building applications with Langchain    
  
[2 LangSmith](Attachments/29CFBDFF-1C94-4C91-8CDC-6BDB555ACFD0.png)  
###  Benefits of Using LangChain   
  
There are several benefits of using LangChain to build applications powered by LLMs. These benefits include:  
* **Ease of use**: LangChain makes it easier to use LLMs to build a variety of applications, even if the developer does not have any experience with artificial intelligence (AI) or machine learning  
* **Flexibility**: LangChain is a flexible framework that can be used to build a wide variety of applications. Developers are not limited to any specific use case  
* **Scalability**: LangChain is scalable to support applications of all sizes. Developers can use LangChain to build applications that serve millions of users  
* **Robustness**: LangChain provides several features that make it easier to build robust and reliable applications. For example, LangChain supports caching and error handling Model I/O LangChain provides an easy out-of-the-box framework to work with LLMs. As shown in the image below, the Model I/O comprises of the following elements:  
* Format the prompt with the help of PromptTemplates  
* Predict the response of the LLM  
* Parse the output response of the LLM    
* Model I/o  
* LangChain provides two methods to work with large language models (LLMs):  
    * LLMs: Models that take a text string as input and return a text string   
    * Chat models: Models that are backed by a language model but take a list of chat messages as input and return a chat message output  
*   LLMs and chat models are subtly similar but differ in their output responses. LLMs in langchain framework refer to pure text completion models - where a string prompt is taken as the input and the language model outputs a string. The completions model of OpenAI API is an example of LLMs supported in Langchain. Chat models are LLMs that have been tuned specifically for having turn-based conversations, such as ChatGPT. Instead of a single string, they take a list of chat messages as input. Usually, these models have labelled messages, such as ‘System’ and ‘Human’ and provide an AI chat message (‘AI’/ ‘Output Response’) as the output. The ChatCompletions model of OpenAI API is an example of a chat model in Langchain.   LangChain provides a standard interface for interacting with many different LLMs to perform standard text completion tasks. The LLM class of LangChain is designed to provide a standard interface for all the major LLM providers, such as OpenAI, Cohere, Hugging Face, etc. It should be noted that the corresponding library must first be installed before being used with langchain. For example, the OpenAI library must first be installed using the   command before you can use the ChatOpenAI class in LangChain. **  **NOTE: If not specified, the default OpenAI completions model used is the text-davinci-003 model whereas the default chat model is gpt-3.5-turbo. It should be noted that the text-davinci-003 model along with other OpenAI completions models has been classified as legacy and is set to retire from active use. The complete list can be referred ++[here](https://platform.openai.com/docs/models/overview)++.   The ‘chat models’ class in Langchain requires the input prompts to be passed in the form of messages. Chat messages in LangChain are a way to interact with the LLM using a chat-like interface. They function similar to a simple text input to the LLM but with a small difference - the chat model must be supplied with message types for the various roles: System, Human and AI.  
    * SystemMessage - System messages provide helpful background context that tells the chat model/AI what to do  
    * HumanMessage - A ChatMessage that represents Human messages/intended to represent the user  
    * AIMessage - AI messages show the chat model’s (/AI) response  
*   A few of the supported chat models in langchain currently are:  
    * ChatOpenAI,  
    * ChatVertexAI,  
    * AzureChatOpenAI,  
    * BedrockChat,  
    * ChatAnthropic,  
    * ChatCohere,  
    * ChatDatabricks,  
    * ChatGooglePalm etc.  
*   An important benefit of using Langchain is the ease with which language models can be swapped in and out. The abstractions provided by Langchain make it easy for developers to compare the performance of both closed and open-source language models for a particular task. For more information on working with open-source language models on HuggingFace, refer to the following link.   **Additional Readings: **In this article, you will come across how different language models can be accessed using this ++[link](https://medium.com/codecontent/using-huggingface-openai-and-cohere-models-with-langchain-0fbf48067764)++    
## Prompt Template    
Prompt templates are a crucial component of LangChain’s framework that makes constructing prompts with dynamic inputs easier. In a traditional API, you would write the prompt query and write a function to perform an API call. In LangChain, an API call to the language model is performed by passing the prompt as a prompt template to a text completion or a chat model.   
  
PromptTemplates are predefined structures for different types of prompts. They serve as a starting point for creating prompts and provide a consistent structure that helps guide the responses of the language model with the added benefit of in-built validation by LangChain.   
  
Prompt Templates can be optimised for diverse applications, such as classification, generation, question-answer, summarization and translation prompts.  The functionality of PromptTemplate can be compared to that of f-strings in Python. A prompt template comprises a string template and accepts a set of parameters from the user that can be used to generate a prompt for a language model and provide ease of use while working with dynamic input prompts. By using PromptTemplates, you can formalise the process of building prompts with an object-oriented approach, add multiple parameters to prompts, build prompts with dynamic inputs, reuse great prompts hundreds of times, streamline the prompt engineering process and provide a consistent structure that helps guide the responses of the language model. This also lets developers create language model agnostic templates to make it easy to reuse existing templates across different language models.  
  
The sequence of steps in defining a PromptTemplate is as follows:  
* **Define the structure of the prompt template**: The first step in creating a Prompt Template is to define the structure of the prompt template. This includes defining the different parts of the prompt, such as instructions, context, user input and output indicator.  
* **Define the parameters of the prompt template**: Once you have defined the structure of the prompt template, you need to define the parameters of the prompt template. These are the variables that will be used to create dynamic prompts.  
* **Use the Prompt Template to generate prompts with dynamic inputs**: Finally, you can use the Prompt Template to generate prompts with dynamic inputs. You can pass in different values for the parameters to create different prompts.  
  
* The ‘langchain.prompts’ class in LangChain allows two methods for easily defining prompt templates:  
* **PromptTemplate**: Create a prompt template for a string prompt. This template only works with the completions model defined by the langchain.llms  
* **ChatPromptTemplate**: Create a prompt template out of a list of chat messages. Each chat message is associated with content and a role. For example, in the OpenAI Chat Completions API, these correspond to system, human or AI roles. It should be noted that this prompt template works when used with chat completions API defined by long chain.chat_models  
  
* An added advantage of using prompt templates is that the same prompt can be used multiple times with different sets of input variables or vice-versa.  
  
  
## OUtput Parsing    
LangChain provides a helpful way to format the output response of a model. These parsers are especially useful when a structured output is required. The official documentation of LangChain contains the list of supported output parsers documentation.  
  
Output Parsers are classes that help structure language model responses. Typically, LLMs output text as responses; however, if you want to get a more structured response than just the response text, Output Parsers are effective.  
  
Output Parsers provide an easy method to restrict the output response of the large language model. The following are a few of the output parsers available in Langchain:  
* BooleanOutputParser,  
* CommaSeparatedListOutputParser,  
* DatetimeOutputParser,  
* ListOutputParser,  
* MarkdownListOutputParser,  
* NumberedListOutputParser,  
* PandasDataFrameOutputParser,  
* RegexDictParser,  
* RegexParser etc.  
  
* Output parsers provide a template to define the output of the language model and effectively consist of a system message that parses the input to the class by performing an API call to the language model. For example, the CommaSeparatedListOutputParser method consists of the following instructions for the LLM:  
```
￼


```
  
```



```
  
```
Your response should be a list of comma separated values, eg:  `foo, bar, baz`



```
  
  
## Document Loaders    
  
So far, you’ve looked at various components offered by Model I/O in langchain. These components provide an easy method of creating Generative AI applications but these models are limited by their training cut-off due to which it can provide insufficient or limited answers to queries regarding current knowledge. This can be rectified using techniques such as Retrieval Augmented Generation (RAG). Langchain provides components for performing RAG through various Retrieval methods as illustrated in the image below.   
  
[Data connection](Attachments/6A775FAF-7B68-46BB-B796-761FCF3D8BE4.png)  
  
 Document loaders in LangChain are responsible for loading documents from different sources and handling various types of documents, including PDFs. They convert these documents into a format that can be processed by the LangChain system, involving steps such as data ingestion, context understanding, and fine-tuning. There are different types of document loaders, such as Transform Loaders, Public Datasets/Services Loaders, and Proprietary Datasets/Services Loaders, which can handle data from specific formats and transform them into the Document format. These document loaders are essential for structuring documents for language model applications and maximising the potential of the LangChain platform  
  
Session Summary  
  
In this course, we focused on getting acquainted with Langchain and some its basic components such as Model I/O (Models such as llms and chat models, PromptTemplates, Output Parsers). At the end of the course, you should be able to understand:  
1. What is Langchain and why is it gaining popularity?  
2. The vaious components of Langchain  
3. Langchain Model I/O Components:  
    * Model  
    * Prompt Templates  
    * Output Parsers  
4. Document Loaders in Langchain  
